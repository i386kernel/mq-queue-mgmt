* My Application Queues
DEFINE QLOCAL('MY.FIRST.QUEUE') +
       DESCR('My first queue via GitOps') +
       DEFPSIST(YES) +
       MAXDEPTH(5000) +
       REPLACE

DEFINE QLOCAL('MY.SECOND.QUEUE') +
       DESCR('My SECOND queue via GitOps') +
       DEFPSIST(YES) +
       MAXDEPTH(5000) +
       REPLACE
